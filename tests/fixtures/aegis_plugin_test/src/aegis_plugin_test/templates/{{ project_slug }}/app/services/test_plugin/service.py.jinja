"""Test plugin's main service module."""


class TestPluginService:
    """Stub service exposed by the test plugin.

    Lives here purely so the round-8 ManualUpdater test can verify
    that plugin-shipped files land at the right relative path under
    the target project's tree.
    """

    project_slug = "{{ project_slug }}"
