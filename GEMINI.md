# Gemini Project-Specific Instructions

When making changes to files inside the `test-scheduler-validation` directory, always check if the same change needs to be applied to the corresponding template file in `aegis/templates/cookiecutter-aegis-project`. This ensures that new projects generated from the template will include the latest updates.
